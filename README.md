# API de Controle de Tasks (To-Do List)

Este projeto é uma API para controle de tasks (To-Do List), desenvolvida utilizando **Spring Boot**. Durante o desenvolvimento, foram abordados conceitos como criação de rotas HTTP, 
validação de parâmetros, integração com banco de dados e autenticação com **JWT** utilizando o **Spring Security**.
